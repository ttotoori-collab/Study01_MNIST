# CLAUDE.md (전역)

모든 프로젝트에 적용되는 공통 지침이다.

## 시각

- 시각은 **한국 표준시(KST, UTC+9)** 로 표시한다. 이 컴퓨터의 로컬 시간대가 KST다.
- 도구가 UTC(`...Z`)나 다른 시간대로 돌려주면 **KST로 변환해서** 보여 준다.
  원래 값이 중요한 경우에만 괄호로 함께 적는다.
  예) GitHub API 가 `2026-09-25T01:08:28Z` 를 주면 `2026-09-25 10:08 KST` 로 적는다.
- 날짜만 쓸 때도 KST 기준 날짜를 쓴다. UTC 기준이면 하루 어긋날 수 있다.
- 특히 다음을 사람에게 보고할 때 지킨다: git 커밋 시각, CI 실행 시각, 로그 타임스탬프,
  파일 수정 시각, 예약 작업 시각.
- 시간대를 알 수 없는 값을 KST 라고 단정하지 않는다. 모르면 모른다고 적는다.
